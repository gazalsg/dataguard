[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "dataguard"
version = "1.0.0"
description = "DataGuard OpenEnv environment"
authors = [{name = "GKK1"}]
dependencies = [
    "pydantic",
    "openenv",
    "fastapi",
    "uvicorn"
]
